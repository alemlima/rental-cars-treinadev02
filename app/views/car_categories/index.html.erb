<h3>Categorias de veículos:</h3><br>

<% @car_categories.empty? %>
  <p>Não existem categorias cadastradas.</p>

<% @car_categories.each do |cc| %>
  <%= link_to cc.name, car_category_path(cc.id)%>
<% end %>

<%= link_to 'Cadastrar nova categoria', new_car_category_path %>
<%= link_to 'Voltar', root_path %>