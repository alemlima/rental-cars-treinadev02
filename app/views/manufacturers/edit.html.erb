<h1>Editar um Fabricante</h1>
<% if @manufacturer.errors.any? %>
<p>Você deve corrigir todos os erros:</p>
  <ul>
    <% @manufacturer.errors.full_messages.each do |message| %>
      <li> <%= message %></li>
    <% end %>
  </ul>
<% end %>

<%= render 'manufacturers_form'%>